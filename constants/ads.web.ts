/** Web builds do not use AdMob — stubs for Metro platform resolution. */

export function getBannerAdUnitId(): string {
  return '';
}

export function getRewardedAdUnitId(): string {
  return '';
}
